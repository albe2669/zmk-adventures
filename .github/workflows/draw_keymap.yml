name: Draw keymap

on:
  push:
    branches:
      - master
    paths:
      - 'config/**.keymap'

permissions:
  contents: write

jobs:
  draw:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install keymap-drawer
        run: pip install keymap-drawer

      - name: Generate keymap SVG
        run: |
          keymap parse -z config/splitkb_aurora_corne.keymap \
            -c keymap_drawer.config.yaml > /tmp/keymap.yaml
          keymap draw /tmp/keymap.yaml \
            -c keymap_drawer.config.yaml > images/keymap.svg

      - name: Commit updated images
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add images/keymap.svg
          git diff --staged --quiet || \
            git commit -m "chore: regenerate keymap images [skip ci]" && \
            git push origin HEAD:master
